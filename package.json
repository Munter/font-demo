{
  "name": "font-demo",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist",
    "build": "cp -r src dist && subfont dist -i --inline-css",
    "surge": "npm run build && surge -d font-demo-subfont.surge.sh dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "subfont": "^2.0.0",
    "surge": "^0.19.0"
  }
}
