{
  "name": "font-demo",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf dist",
    "build": "cp -r src dist",
    "surge": "npm run build && surge -d font-demo-baseline.surge.sh dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "surge": "^0.19.0"
  }
}
